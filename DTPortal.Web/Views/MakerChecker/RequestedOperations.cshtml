@model IEnumerable<DTPortal.Web.ViewModel.MakerChecker.MakerCheckerListViewModel>

@{ ViewData["Title"] = "List"; }



<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body ">
                <div class="row p-b-20">
                    <div class="col-md-6 col-sm-6 col-6">
                        <div class="btn-group">
                            <button type="button" onClick="window.location.reload();" href="" class="btn btn-info">
                                Refresh
                            </button>
                        </div>
                    </div>

                </div>
                <table id="roleTable" class="table table-striped table-bordered table-hover table-checkable order-column full-width">
                    <thead>
                        <tr>
                            <th>Operation type</th>
                            <th>Activity Name</th>
                            <th>Created Date</th>
                            <th>Status</th>
                            <th>Comment</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model == null || Model.Count() == 0)
                        {
                            <tr>
                                <td style="text-align:center" valign="top" colspan="5" class="dataTables_empty">No record found</td>
                            </tr>
                        }
                        else
                        {
                            @foreach (var item in Model)
                            {
                                <tr class="odd gradeX">
                                    <td>@item.ApprovalList.OperationType</td>
                                    <td>@item.OperationName</td>
                                    <td>@item.ApprovalList.ModifiedDate</td>
                                    <td>@item.ApprovalList.State</td>
                                    <td>@item.ApprovalList.Comment</td>
                                    @*<td class="valigntop">


                                            <div class="btn-group">
                                                <button class="btn btn-xs deepPink-bgcolor no-margin" type="button" data-toggle="dropdown"
                                                        aria-expanded="false">
                                                    Actions
                                                    <i class="fa fa-angle-down"></i>
                                                </button>
                                                <ul class="dropdown-menu pull-left" >
                                                    <li>
                                                        <a href="@Url.Action("View", "MakerChecker", new { id = item.Id })">
                                                            <i class="icon-docs"></i>View
                                                        </a>
                                                        <a href="@Url.Action("View", "MakerChecker", new { id = item.Id })">
                                                            <i class="icon-docs"></i>View
                                                        </a>
                                                        <a href="@Url.Action("View", "MakerChecker", new { id = item.Id })">
                                                            <i class="icon-docs"></i>View
                                                        </a>
                                                    </li>
                                                </ul>
                                            </div>
                                        </td>*@
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
@section scripts{
    <script>

        $(document).ready(function () {

            $('#dashboardMenu').addClass('active');

            /*For Beadcrum Start*/
            $('#homeMenu').addClass('breadcrumb-item').append('Dashboard');
            $('#mainMenu').addClass('breadcrumb-item active').append('Requested Operations');
            $('#subMenu').removeClass('breadcrumb-item');

            //var table = $("#example4").DataTable({
            //    "bLengthChange": false,
            //});

            $(".table").DataTable({
                "bLengthChange": false,
                "aaSorting" : []
            });

            $('#roleTable').on('click', '.js-delete', function () {
                var deleteButton = $(this);
                var data = $('#roleTable').DataTable().row(deleteButton.parents("tr")).data();
                if (data[0] == "Admin" || data[0] == "Admin Checker") {
                    alert('The role ' + data[0] + ' is default role, You cant not delete this role');
                    return false;
                }
                var id = deleteButton.attr("data-id");
                if (confirm("Are you sure you want to delete role with " + id)) {
                    $.ajax({
                        type: 'POST',
                        cache: false,
                        dataType: 'json',
                        url: '@Url.Action("Delete", "RoleManagement")',
                        data: {
                             id: id
                        },
                        success: function () {
                            $("#roleTable").DataTable().row(deleteButton.parents("tr")).remove().draw();
                        },
                        error: function (response) {
                            if (response.statusText == "Unauthorized") {
                                swal({ type: 'info', title: "Session Expired", text: "Click on 'Ok' button for login again!" }, function (isConfirm) {
                                    if (isConfirm) {
                                        window.location.href = '@Url.Action("Index", "Login")'
                                    }
                                });
                            } else {
                                alert('Failed to delete role.');
                            }
                        }
                    });
                }
            });
        });

    </script>
}
